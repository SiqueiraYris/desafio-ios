public struct RouterURLs {
    public static let login = RouterHost.url.appending(RouterPaths.login)
    public static let statement = RouterHost.url.appending(RouterPaths.statement)
    public static let launcher = RouterHost.url.appending(RouterPaths.launcher)
}
