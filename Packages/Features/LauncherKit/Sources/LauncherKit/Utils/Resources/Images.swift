import UIKit

struct Images {
    static let background = UIImage(named: "background", in: .module, compatibleWith: nil)
    static let logo = UIImage(named: "logo", in: .module, compatibleWith: nil)
    static let arrowRight = UIImage(systemName: "arrow.right")
}
